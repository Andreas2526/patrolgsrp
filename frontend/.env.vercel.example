# Frontend (Vercel) runtime configuration
# Railway backend base URL, no trailing slash
NEXT_PUBLIC_API_BASE_URL=https://your-backend.up.railway.app

# Optional: expose Supabase values to frontend only if needed by future UI features
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Helpful reference for Discord OAuth app setup
# Discord redirect URI should point to backend callback endpoint:
# https://your-backend.up.railway.app/auth/discord/callback
